{
  "$id": "round_started_event.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RoundStartedEvent",
  "description": "Event occurring when a new round has started.",
  "type": "object",
  "extends": {
    "$ref": "message.json"
  },
  "properties": {
    "roundNumber": {
      "description": "The current round number in the battle when event occurred",
      "type": "integer"
    }
  },
  "required": [
    "roundNumber"
  ]
}
